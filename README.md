# 4444 Project

# Download miniconda
# Look up “anaconda prompt” on the computer search bar.
# “conda create -n mlagents python=3.10.12” in the prompt.
# “conda activate mlagents” in the prompt
# “pip install mlagents” 
# “cd C:\Users\Ravi\Documents\RPG Tutorial\4444-Project” or “cd C:\Users\...\4444-Project” Where the project is downloaded to. 
# “mlagents-learn config/ppo/MoveToGoal.yaml --run-id=test4” To make a test run. Change the id to a unique one or use ”--force” to replace one. 
